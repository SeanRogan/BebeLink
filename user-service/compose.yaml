services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=postgres-bebelink-user'
      - 'POSTGRES_PASSWORD=postpass'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'